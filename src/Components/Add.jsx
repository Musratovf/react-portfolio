import { Tooltip } from '@mui/material';
import React from 'react';

const Add = () => {
	return (
		<>
			<Tooltip title='Delete'>
                
            </Tooltip>
		</>
	);
};

export default Add;
